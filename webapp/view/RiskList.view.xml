<mvc:View controllerName="ehsmportal.controller.RiskList"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m">
    <Page id="riskListPage" title="Risk Assessment" showNavButton="true" navButtonPress=".onNavBack">
        <content>
            <Table id="riskTable"
                items="{
                    path: 'riskModel>/results',
                    sorter: { path: 'RiskIdentificationDate', descending: true }
                }"
                mode="SingleSelectMaster"
                itemPress=".onRiskPress"
                growing="true"
                growingThreshold="20">
                <headerToolbar>
                    <Toolbar>
                        <Title text="All Risks" level="H2"/>
                    </Toolbar>
                </headerToolbar>
                <columns>
                    <Column>
                        <Text text="Risk ID" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="Category" />
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="Description" />
                    </Column>
                    <Column>
                        <Text text="Severity" />
                    </Column>
                    <Column hAlign="End">
                        <Text text="Likelihood" />
                    </Column>
                </columns>
                <items>
                    <ColumnListItem type="Navigation">
                        <cells>
                            <ObjectIdentifier title="{riskModel>RiskId}" text="{riskModel>Plant}" />
                            <Text text="{riskModel>RiskCategory}" />
                            <Text text="{riskModel>RiskDescription}" maxLines="1" />
                            <ObjectStatus text="{riskModel>RiskSeverity}" state="{= ${riskModel>RiskSeverity} === 'High' ? 'Error' : ${riskModel>RiskSeverity} === 'Medium' ? 'Warning' : 'Success' }" />
                            <Text text="{riskModel>Likelihood}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
    </Page>
</mvc:View>
